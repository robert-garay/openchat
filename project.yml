name: OpenChat
options:
  bundleIdPrefix: com.openchat
  deploymentTarget:
    iOS: "17.0"
  createIntermediateGroups: true
  groupSortPosition: top

configs:
  Debug: debug
  Release: release

settings:
  base:
    SWIFT_VERSION: "6.0"
    IPHONEOS_DEPLOYMENT_TARGET: "17.0"
    TARGETED_DEVICE_FAMILY: "1,2"
    SWIFT_APPROACHABLE_CONCURRENCY: YES
    ENABLE_PREVIEWS: YES
    DEVELOPMENT_TEAM: ""
    CODE_SIGN_STYLE: Automatic
    MARKETING_VERSION: "1.0"
    CURRENT_PROJECT_VERSION: "1"

targets:
  OpenChat:
    type: application
    platform: iOS
    sources:
      - path: OpenChat
        excludes:
          - "Resources/Info.plist"
    info:
      path: OpenChat/Resources/Info.plist
      properties:
        CFBundleDisplayName: OpenChat
        CFBundleShortVersionString: $(MARKETING_VERSION)
        CFBundleVersion: $(CURRENT_PROJECT_VERSION)
        LSRequiresIPhoneOS: true
        UILaunchScreen: {}
        UISupportedInterfaceOrientations:
          - UIInterfaceOrientationPortrait
          - UIInterfaceOrientationLandscapeLeft
          - UIInterfaceOrientationLandscapeRight
        UISupportedInterfaceOrientations~ipad:
          - UIInterfaceOrientationPortrait
          - UIInterfaceOrientationPortraitUpsideDown
          - UIInterfaceOrientationLandscapeLeft
          - UIInterfaceOrientationLandscapeRight
        UIUserInterfaceStyle: Automatic
        ITSAppUsesNonExemptEncryption: false
        NSAppTransportSecurity:
          NSAllowsArbitraryLoads: true
        NSHealthShareUsageDescription: OpenChat reads Apple Health fitness data you enable so agents can provide workout and recovery insights. This is not medical advice.
        NSCameraUsageDescription: OpenChat uses the camera so agents can analyze photos or documents you capture.
        NSMicrophoneUsageDescription: OpenChat uses the microphone for voice input and transcription when you enable it.
        NSPhotoLibraryUsageDescription: OpenChat accesses photos you select so agents can analyze images.
        NSCalendarsFullAccessUsageDescription: OpenChat reads your calendar so agents can help with schedules and meeting prep.
    entitlements:
      path: OpenChat/Resources/OpenChat.entitlements
      properties:
        com.apple.developer.healthkit: true
    settings:
      base:
        PRODUCT_NAME: OpenChat
        PRODUCT_BUNDLE_IDENTIFIER: com.openchat.app
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        ASSETCATALOG_COMPILER_GLOBAL_ACCENT_COLOR_NAME: AccentColor
        ASSETCATALOG_COMPILER_INCLUDE_ALL_APPICON_ASSETS: YES
        ENABLE_HARDENED_RUNTIME: YES
        SUPPORTS_MACCATALYST: NO
    capabilities: {}

  OpenChatTests:
    type: bundle.unit-test
    platform: iOS
    sources:
      - path: OpenChatTests
    dependencies:
      - target: OpenChat
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.openchat.app.tests

schemes:
  OpenChat:
    build:
      targets:
        OpenChat: all
        OpenChatTests: [test]
    run:
      config: Debug
    test:
      config: Debug
      targets:
        - OpenChatTests
    archive:
      config: Release
