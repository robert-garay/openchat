// Test target signing. Unit-test bundles do not need to be signed for local
// simulator builds; keeping this off avoids "No Account for Team" build errors.

CODE_SIGNING_ALLOWED = NO

#include? "Local.xcconfig"
