name: Deploy GitHub Pages

on:
  push:
    branches:
      - main
    paths:
      - website/**
      - docs/privacy-policy.html
      - .github/workflows/pages.yml
      - OpenChat/Resources/Assets.xcassets/OpenChatLogo.imageset/**
      - OpenChat/Resources/SharedAssets.xcassets/OpenChatMark.imageset/**
      - OpenChat/Resources/Assets.xcassets/AppIcon.appiconset/**
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Prepare site
        run: |
          mkdir -p _site
          cp website/index.html _site/index.html
          cp website/brand.css _site/brand.css
          cp docs/privacy-policy.html _site/privacy-policy.html
          cp -R website/assets _site/assets
          cp -R website/wiki _site/wiki

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: _site

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
